package org.watsi.device.db.daos

import org.junit.Test

class MainDaoTest : DaoBaseTest() {

    @Test
    fun deleteAll() {
        // let's create lots of records with associations, and then make sure we can delete them all.
    }
}
