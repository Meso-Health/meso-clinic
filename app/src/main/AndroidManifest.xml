<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.watsi.uhp">

    <uses-feature android:name="android.hardware.camera" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:launchMode="singleTop"
        android:theme="@style/main_theme">
        <provider
            android:name=".providers.MemberContentProvider"
            android:authorities="org.watsi.uhp.provider.member" />
        <service
            android:name=".services.RefreshMemberListService"
            android:exported="false" />
        <service
            android:name=".services.SyncService"
            android:exported="false" />
        <activity
            android:windowSoftInputMode="adjustPan"
            android:name=".activities.MainActivity"
            android:label="@string/main_activity_label"
            android:launchMode="singleTop" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.SEARCH" />
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable_member" />
        </activity>
    </application>
</manifest>